apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nussknacker.fullname" . }}-test-bats-suite
  labels:
  {{- include "nussknacker.labels" . | nindent 4 }}
data:
  sample.bats: |-
    #!/usr/bin/env bats
    @test "shoud handle requests" {
      wget {{ include "nussknacker.fullname" . }}:{{ .Values.service.port }}
    }
